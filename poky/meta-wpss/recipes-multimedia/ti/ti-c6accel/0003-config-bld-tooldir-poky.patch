diff --git a/config.bld b/config.bld
index 3310179..5bc4010 100755
--- a/config.bld
+++ b/config.bld
@@ -61,15 +61,22 @@ var longName = "";
    else leave the tooldir as "" and set the LONGNAME to the full CROSS_COMPILE
    path
 */
-var regex = new RegExp("bin/");
+var regex = new RegExp("/");
 var find = crosscompile.split( regex );
+var regex2 = new RegExp(find[find.length - 1]);
+var find2 = crosscompile.split( regex2 );
 
+/*
 if (find[0]!=crosscompile && find.length==2) {
     tooldir = find[0];
     longName = "bin/" + find[1] + "gcc";
 } else {
     longName = crosscompile + "gcc";
 }
+*/
+longName = find[find.length-1] + "gcc";
+tooldir = find2[0] ;
+
 
 /* location of the GCC Arm9 tools */
 var GCArmv5T = xdc.useModule('gnu.targets.arm.GCArmv5T');
